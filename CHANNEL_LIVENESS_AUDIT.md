# Channel liveness audit

Audit date: 2026-09-11. Repository: `trusted-competency-renew-model`; branch: `feat/renew-functional-integration`; inspected commit: `b9f19c2618904646d2dc712a2620eebeb5a4e907`.

Scope: the saved 11 RNW drawings, compiled and replayed in Renew 4.1 / Java 17. Native deserialization verified every node, normal directed arc, and stored text against `model.tsv`; the manifest was independently reconciled with that TSV. No model, marking, inscription, project script, Java helper, README, or existing validation document was edited. An external temporary observer in `../.validation/deadlock_audit` replayed the existing directed sequences and recorded additional continuations and enabled bindings. Reports are the only repository additions.

**EXHAUSTIVE STATE-SPACE ANALYSIS NOT PERFORMED**

All reachability claims below concern witnessed directed executions or explicitly identified structural deductions. Endpoint binding searches inspect the current configuration; they do not enumerate its reachable state space, prove boundedness, or prove liveness. Time is the carried `currentTime` integer, not a clock that advances while waiting. Signatures are matching placeholder strings, not cryptography.

## Interpretation

- **Local deadlock:** an instance has no enabled local or synchronized transition while the rest of the system may still progress. A temporary peer wait also has no current binding; it is distinguished from a permanently blocked, unfinished instance.
- **Global deadlock:** no transition in the entire reachable configuration can fire. If this is because every activated lifecycle ended coherently, classify it as expected termination instead of a defect.
- **Expected termination:** successful or valid failure sinks, plus conditional peers that never received a request. A sink alone is not a deadlock finding.
- **Protocol wait:** a required peer event may still occur. **Permanent wait:** that event cannot occur again in this execution.
- A transition with a token and satisfied local guard can still lack a synchronous binding. Such a transition is **locally ready but disabled**, not literally “enabled-but-unsynchronizable.” No contradictory full-enabledness claim is made.
- A reference-reachable but inactive object is not an orphan. Root pool tokens retain the eight agents; EvidenceNet and CompetencyNet retain their created objects.

## Verified channel incidences

The table contains 36 caller/receiver incidences, including setup channels, object channels, and both guarded EvidenceNet evaluate receivers. It is not limited to seven business names. Every downlink resolves to an existing uplink on the referenced net with the same arity. `CHANNEL_MATRIX.md` was cross-checked against inscriptions; the rows below were reconstructed from the verified model data.

Reference map: `this` = caller instance; `s` StudentAgent, `p` ProfessorAgent, `u` UniversityAgent, `e` EvidenceNet, `c` CompetencyNet, `w` WalletNet, `l` HEDULedgerNet, `h` HRAgent, `eo` EvidenceObject, `co` CredentialObject. Wallet receives `c` as CompetencyNet's `this` at minting. Root references are created once and carried in tuple tokens; object references are produced by `:new` and retained in their parent token.

Each enabled-condition cell gives the source/destination, exact input/output arc patterns, and full transition inscription. Tokens must match these patterns; shared variables and all positional channel arguments must unify. All guards, nested downlinks and uplinks shown must be satisfied **together**. Uplink transitions do not initiate autonomously. A two-endpoint match alone does not make a multiway transaction fireable.

| Channel | Caller Net | Caller Transition | Receiver Net | Receiver Transition | Caller Enabled Conditions | Receiver Enabled Conditions | Can Become Unmatched? | Failure Scenario | Impact |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| createp/1; this:createp(p) ↔ :createp(p) | SystemNet.rnw | t_CreateStudent | SystemNet.rnw | t_CreateProfessor | p_StudentPool → p_StudentPool; input `[]`; output `s`; `s :new StudentAgent; this:createp(p); this:createu(u); this:createe(e); this:createc(c); this:createw(w); this:createl(l); this:createh(h); s:setup(p,u,e,c,w,l,h)` | p_ProfessorPool → p_ProfessorPool; input `[]`; output `p`; `p :new ProfessorAgent; :createp(p)` | No permanent mismatch witnessed | Target reference, receiver, arguments and prerequisite markings match in the recorded invocation. | Released in directed replay; this is not a universal liveness proof. |
| createu/1; this:createu(u) ↔ :createu(u) | SystemNet.rnw | t_CreateStudent | SystemNet.rnw | t_CreateUniversity | p_StudentPool → p_StudentPool; input `[]`; output `s`; `s :new StudentAgent; this:createp(p); this:createu(u); this:createe(e); this:createc(c); this:createw(w); this:createl(l); this:createh(h); s:setup(p,u,e,c,w,l,h)` | p_UniversityPool → p_UniversityPool; input `[]`; output `u`; `u :new UniversityAgent; :createu(u)` | No permanent mismatch witnessed | Target reference, receiver, arguments and prerequisite markings match in the recorded invocation. | Released in directed replay; this is not a universal liveness proof. |
| createe/1; this:createe(e) ↔ :createe(e) | SystemNet.rnw | t_CreateStudent | SystemNet.rnw | t_CreateEvidence | p_StudentPool → p_StudentPool; input `[]`; output `s`; `s :new StudentAgent; this:createp(p); this:createu(u); this:createe(e); this:createc(c); this:createw(w); this:createl(l); this:createh(h); s:setup(p,u,e,c,w,l,h)` | p_EvidencePool → p_EvidencePool; input `[]`; output `e`; `e :new EvidenceNet; :createe(e)` | No permanent mismatch witnessed | Target reference, receiver, arguments and prerequisite markings match in the recorded invocation. | Released in directed replay; this is not a universal liveness proof. |
| createc/1; this:createc(c) ↔ :createc(c) | SystemNet.rnw | t_CreateStudent | SystemNet.rnw | t_CreateCompetency | p_StudentPool → p_StudentPool; input `[]`; output `s`; `s :new StudentAgent; this:createp(p); this:createu(u); this:createe(e); this:createc(c); this:createw(w); this:createl(l); this:createh(h); s:setup(p,u,e,c,w,l,h)` | p_CompetencyPool → p_CompetencyPool; input `[]`; output `c`; `c :new CompetencyNet; :createc(c)` | No permanent mismatch witnessed | Target reference, receiver, arguments and prerequisite markings match in the recorded invocation. | Released in directed replay; this is not a universal liveness proof. |
| createw/1; this:createw(w) ↔ :createw(w) | SystemNet.rnw | t_CreateStudent | SystemNet.rnw | t_CreateWallet | p_StudentPool → p_StudentPool; input `[]`; output `s`; `s :new StudentAgent; this:createp(p); this:createu(u); this:createe(e); this:createc(c); this:createw(w); this:createl(l); this:createh(h); s:setup(p,u,e,c,w,l,h)` | p_WalletPool → p_WalletPool; input `[]`; output `w`; `w :new WalletNet; :createw(w)` | No permanent mismatch witnessed | Target reference, receiver, arguments and prerequisite markings match in the recorded invocation. | Released in directed replay; this is not a universal liveness proof. |
| createl/1; this:createl(l) ↔ :createl(l) | SystemNet.rnw | t_CreateStudent | SystemNet.rnw | t_CreateLedger | p_StudentPool → p_StudentPool; input `[]`; output `s`; `s :new StudentAgent; this:createp(p); this:createu(u); this:createe(e); this:createc(c); this:createw(w); this:createl(l); this:createh(h); s:setup(p,u,e,c,w,l,h)` | p_LedgerPool → p_LedgerPool; input `[]`; output `l`; `l :new HEDULedgerNet; :createl(l)` | No permanent mismatch witnessed | Target reference, receiver, arguments and prerequisite markings match in the recorded invocation. | Released in directed replay; this is not a universal liveness proof. |
| createh/1; this:createh(h) ↔ :createh(h) | SystemNet.rnw | t_CreateStudent | SystemNet.rnw | t_CreateHR | p_StudentPool → p_StudentPool; input `[]`; output `s`; `s :new StudentAgent; this:createp(p); this:createu(u); this:createe(e); this:createc(c); this:createw(w); this:createl(l); this:createh(h); s:setup(p,u,e,c,w,l,h)` | p_HRPool → p_HRPool; input `[]`; output `h`; `h :new HRAgent; :createh(h)` | No permanent mismatch witnessed | Target reference, receiver, arguments and prerequisite markings match in the recorded invocation. | Released in directed replay; this is not a universal liveness proof. |
| setup/7; s:setup(p,u,e,c,w,l,h) ↔ :setup(p,u,e,c,w,l,h) | SystemNet.rnw | t_CreateStudent | StudentAgent.rnw | t_PrepareEvidence | p_StudentPool → p_StudentPool; input `[]`; output `s`; `s :new StudentAgent; this:createp(p); this:createu(u); this:createe(e); this:createc(c); this:createw(w); this:createl(l); this:createh(h); s:setup(p,u,e,c,w,l,h)` | p_Idle → p_EvidenceReady; input `[grade,passing_threshold,currentTime,expiryDate,studentDID,evidenceCID,studentSignature]`; output `[p,u,e,c,w,l,h,grade,passing_threshold,currentTime,expiryDate,studentDID,evidenceCID,studentSignature]`; `:setup(p,u,e,c,w,l,h)` | No permanent mismatch witnessed | Target reference, receiver, arguments and prerequisite markings match in the recorded invocation. | Released in directed replay; this is not a universal liveness proof. |
| submit/10; p:submit(u,c,w,l,h,e,grade,passing_threshold,currentTime,expiryDate) ↔ :submit(u,c,w,l,h,e,grade,passing_threshold,currentTime,expiryDate) | StudentAgent.rnw | t_SubmitEvidence | ProfessorAgent.rnw | t_ReceiveEvidence | p_EvidenceReady → p_Submitted; input `[p,u,e,c,w,l,h,grade,passing_threshold,currentTime,expiryDate,studentDID,evidenceCID,studentSignature]`; output `[p,u,e,c,w,l,h,grade,passing_threshold,currentTime,expiryDate,studentDID,evidenceCID,studentSignature]`; `p:submit(u,c,w,l,h,e,grade,passing_threshold,currentTime,expiryDate); e:submit(studentDID,evidenceCID)` | p_Idle → p_EvidenceReceived; input `[assessorDID,criteriaURL]`; output `[u,c,w,l,h,e,grade,passing_threshold,currentTime,expiryDate,assessorDID,criteriaURL]`; `:submit(u,c,w,l,h,e,grade,passing_threshold,currentTime,expiryDate)` | No permanent mismatch witnessed | Target reference, receiver, arguments and prerequisite markings match in the recorded invocation. | Released in directed replay; this is not a universal liveness proof. |
| submit/2; e:submit(studentDID,evidenceCID) ↔ :submit(studentDID,evidenceCID) | StudentAgent.rnw | t_SubmitEvidence | EvidenceNet.rnw | t_IPFS_Upload | p_EvidenceReady → p_Submitted; input `[p,u,e,c,w,l,h,grade,passing_threshold,currentTime,expiryDate,studentDID,evidenceCID,studentSignature]`; output `[p,u,e,c,w,l,h,grade,passing_threshold,currentTime,expiryDate,studentDID,evidenceCID,studentSignature]`; `p:submit(u,c,w,l,h,e,grade,passing_threshold,currentTime,expiryDate); e:submit(studentDID,evidenceCID)` | p_Created → p_UploadedIPFS; input `passing_threshold`; output `[eo,studentDID,evidenceCID,passing_threshold]`; `:submit(studentDID,evidenceCID); eo :new EvidenceObject; eo:submit(studentDID,evidenceCID)` | No permanent mismatch witnessed | Target reference, receiver, arguments and prerequisite markings match in the recorded invocation. | Released in directed replay; this is not a universal liveness proof. |
| consent/0; w:consent() ↔ :consent() | StudentAgent.rnw | t_ReceiveCredential | WalletNet.rnw | t_RequestConsent | p_WaitingCredential → p_CredentialReceived; input `[p,u,e,c,w,l,h,grade,passing_threshold,currentTime,expiryDate,studentDID,evidenceCID,studentSignature]`; output `[p,u,e,c,w,l,h,grade,passing_threshold,currentTime,expiryDate,studentDID,evidenceCID,studentSignature]`; `w:consent()` | p_CredentialReceived → p_PendingConsent; input `[c,issuerDID,studentDID,credentialData]`; output `[c,issuerDID,studentDID,credentialData]`; `:consent()` | Yes — absent mint | Evidence rejection leaves Wallet.p_Empty permanently. | Student.p_WaitingCredential has no response. Cancel after mint does not itself prevent consent. |
| sbt_accept/1; w:sbt_accept(studentSignature) ↔ :sbt_accept(studentSignature) | StudentAgent.rnw | t_AcceptCredential | WalletNet.rnw | t_AcceptCredential | p_CredentialReceived → p_Accepted; input `[p,u,e,c,w,l,h,grade,passing_threshold,currentTime,expiryDate,studentDID,evidenceCID,studentSignature]`; output `[p,u,e,c,w,l,h,grade,passing_threshold,currentTime,expiryDate,studentDID,evidenceCID,studentSignature]`; `w:sbt_accept(studentSignature)` | p_PendingConsent → p_Accepted; input `[c,issuerDID,studentDID,credentialData]`; output `[c,issuerDID,studentDID,credentialData]`; `:sbt_accept(studentSignature); c:sbt_accept(studentSignature)` | Yes — transitive loss or argument mismatch | Cancel/expire removes Competency/credential pending token; bad student signature fails CredentialObject equality. Acceptance also requires currentTime < expiryDate. | Student/Wallet decision chain cannot fire; rejection is also blocked by a wrong signature. |
| sbt_reject/1; w:sbt_reject(studentSignature) ↔ :sbt_reject(studentSignature) | StudentAgent.rnw | t_RejectCredential | WalletNet.rnw | t_RejectCredential | p_CredentialReceived → p_Rejected; input `[p,u,e,c,w,l,h,grade,passing_threshold,currentTime,expiryDate,studentDID,evidenceCID,studentSignature]`; output `[p,u,e,c,w,l,h,grade,passing_threshold,currentTime,expiryDate,studentDID,evidenceCID,studentSignature]`; `w:sbt_reject(studentSignature)` | p_PendingConsent → p_Rejected; input `[c,issuerDID,studentDID,credentialData]`; output `[c,issuerDID,studentDID,credentialData]`; `:sbt_reject(studentSignature); c:sbt_reject(studentSignature)` | Yes — transitive loss or argument mismatch | Cancel/expire removes Competency/credential pending token; bad student signature fails CredentialObject equality. Acceptance also requires currentTime < expiryDate. | Student/Wallet decision chain cannot fire; rejection is also blocked by a wrong signature. |
| submit/0; h:submit() ↔ :submit() | StudentAgent.rnw | t_ShareVP | HRAgent.rnw | t_ReceiveVP | p_PresentationReady → p_VPShared; input `[p,u,e,c,w,l,h,grade,passing_threshold,currentTime,expiryDate,studentDID,evidenceCID,studentSignature]`; output `[p,u,e,c,w,l,h,grade,passing_threshold,currentTime,expiryDate,studentDID,evidenceCID,studentSignature]`; `h:submit()` | p_Idle → p_VPReceived; input `[]`; output `[]`; `:submit()` | No unfinished caller at the tested skipped invocation | Upstream rejection/cancellation prevents this request ever being sent. | Receiver remains initial and intentionally inactive; no standalone channel-name defect. |
| evaluate/3; e:evaluate(assessorDID,grade,criteriaURL) ↔ :evaluate(assessorDID,grade,criteriaURL) | ProfessorAgent.rnw | t_AssessEvidence | EvidenceNet.rnw | t_Grade_Assign | p_UnderReview → p_Evaluated; input `[u,c,w,l,h,e,grade,passing_threshold,currentTime,expiryDate,assessorDID,criteriaURL]`; output `[u,c,w,l,h,e,grade,passing_threshold,currentTime,expiryDate,assessorDID,criteriaURL]`; `e:evaluate(assessorDID,grade,criteriaURL)` | p_UnderReview → p_Graded; input `[eo,studentDID,evidenceCID,passing_threshold]`; output `[eo,studentDID,evidenceCID,passing_threshold,assessorDID,grade,criteriaURL]`; `:evaluate(assessorDID,grade,criteriaURL); guard grade >= passing_threshold; eo:evaluate(assessorDID,grade,criteriaURL)` | No in tested fixtures | Grade guards select one of two receivers; EvidenceNet threshold is separately seeded to 50 and matches the forwarded threshold in these fixtures. | Assessment completes in happy and grade_reject; failing branch omits the EvidenceObject notification. |
| evaluate/3; e:evaluate(assessorDID,grade,criteriaURL) ↔ :evaluate(assessorDID,grade,criteriaURL) | ProfessorAgent.rnw | t_AssessEvidence | EvidenceNet.rnw | t_Reject | p_UnderReview → p_Evaluated; input `[u,c,w,l,h,e,grade,passing_threshold,currentTime,expiryDate,assessorDID,criteriaURL]`; output `[u,c,w,l,h,e,grade,passing_threshold,currentTime,expiryDate,assessorDID,criteriaURL]`; `e:evaluate(assessorDID,grade,criteriaURL)` | p_UnderReview → p_Rejected; input `[eo,studentDID,evidenceCID,passing_threshold]`; output `[eo,studentDID,evidenceCID,passing_threshold,assessorDID,grade,criteriaURL]`; `:evaluate(assessorDID,grade,criteriaURL); guard grade < passing_threshold` | No in tested fixtures | Grade guards select one of two receivers; EvidenceNet threshold is separately seeded to 50 and matches the forwarded threshold in these fixtures. | Assessment completes in happy and grade_reject; failing branch omits the EvidenceObject notification. |
| submit/8; u:submit(c,w,l,h,grade,passing_threshold,currentTime,expiryDate) ↔ :submit(c,w,l,h,grade,passing_threshold,currentTime,expiryDate) | ProfessorAgent.rnw | t_ApproveEvidence | UniversityAgent.rnw | t_ReceiveEvidence | p_Evaluated → p_Approved; input `[u,c,w,l,h,e,grade,passing_threshold,currentTime,expiryDate,assessorDID,criteriaURL]`; output `[u,c,w,l,h,e,grade,passing_threshold,currentTime,expiryDate,assessorDID,criteriaURL]`; `guard grade >= passing_threshold; u:submit(c,w,l,h,grade,passing_threshold,currentTime,expiryDate)` | p_Idle → p_EvidenceReceived; input `[issuerDID,studentDID,credentialData]`; output `[c,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData]`; `:submit(c,w,l,h,grade,passing_threshold,currentTime,expiryDate)` | No unfinished caller at the tested skipped invocation | Upstream rejection/cancellation prevents this request ever being sent. | Receiver remains initial and intentionally inactive; no standalone channel-name defect. |
| submit/7; c:submit(w,l,h,grade,passing_threshold,currentTime,expiryDate) ↔ :submit(w,l,h,grade,passing_threshold,currentTime,expiryDate) | UniversityAgent.rnw | t_RequestCredential | CompetencyNet.rnw | t_Review_Init | p_Verifying → p_CredentialRequested; input `[c,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData]`; output `[c,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData]`; `guard grade >= passing_threshold; c:submit(w,l,h,grade,passing_threshold,currentTime,expiryDate)` | p_Submitted → p_UnderReview; input `[issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; output `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; `:submit(w,l,h,grade,passing_threshold,currentTime,expiryDate); co :new CredentialObject; co:evaluate(grade,passing_threshold,currentTime,expiryDate)` | No unfinished caller at the tested skipped invocation | Upstream rejection/cancellation prevents this request ever being sent. | Receiver remains initial and intentionally inactive; no standalone channel-name defect. |
| sbt_mint/3; c:sbt_mint(issuerDID,studentDID,credentialData) ↔ :sbt_mint(issuerDID,studentDID,credentialData) | UniversityAgent.rnw | t_IssueCredential | CompetencyNet.rnw | t_SBT_Mint | p_CredentialRequested → p_CredentialIssued; input `[c,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData]`; output `[c,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData]`; `c:sbt_mint(issuerDID,studentDID,credentialData)` | p_Validated → p_PendingAccept; input `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; output `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; `:sbt_mint(issuerDID,studentDID,credentialData); co:sbt_mint(issuerDID,studentDID,credentialData); w:sbt_mint(this,issuerDID,studentDID,credentialData)` | No permanent mismatch witnessed | Target reference, receiver, arguments and prerequisite markings match in the recorded invocation. | Released in directed replay; this is not a universal liveness proof. |
| submit/2; eo:submit(studentDID,evidenceCID) ↔ :submit(studentDID,evidenceCID) | EvidenceNet.rnw | t_IPFS_Upload | EvidenceObject.rnw | t_IPFS_Upload | p_Created → p_UploadedIPFS; input `passing_threshold`; output `[eo,studentDID,evidenceCID,passing_threshold]`; `:submit(studentDID,evidenceCID); eo :new EvidenceObject; eo:submit(studentDID,evidenceCID)` | p_Created → p_UploadedIPFS; input `[]`; output `[studentDID,evidenceCID]`; `:submit(studentDID,evidenceCID)` | No permanent mismatch witnessed | Target reference, receiver, arguments and prerequisite markings match in the recorded invocation. | Released in directed replay; this is not a universal liveness proof. |
| lms/0; eo:lms() ↔ :lms() | EvidenceNet.rnw | t_LMS_Submit | EvidenceObject.rnw | t_LMS_Submit | p_UploadedIPFS → p_SubmittedLMS; input `[eo,studentDID,evidenceCID,passing_threshold]`; output `[eo,studentDID,evidenceCID,passing_threshold]`; `eo:lms()` | p_UploadedIPFS → p_SubmittedLMS; input `[studentDID,evidenceCID]`; output `[studentDID,evidenceCID]`; `:lms()` | No permanent mismatch witnessed | Target reference, receiver, arguments and prerequisite markings match in the recorded invocation. | Released in directed replay; this is not a universal liveness proof. |
| evaluate/3; eo:evaluate(assessorDID,grade,criteriaURL) ↔ :evaluate(assessorDID,grade,criteriaURL) | EvidenceNet.rnw | t_Grade_Assign | EvidenceObject.rnw | t_Grade_Assign | p_UnderReview → p_Graded; input `[eo,studentDID,evidenceCID,passing_threshold]`; output `[eo,studentDID,evidenceCID,passing_threshold,assessorDID,grade,criteriaURL]`; `:evaluate(assessorDID,grade,criteriaURL); guard grade >= passing_threshold; eo:evaluate(assessorDID,grade,criteriaURL)` | p_SubmittedLMS → p_Graded; input `[studentDID,evidenceCID]`; output `[studentDID,evidenceCID,assessorDID,grade,criteriaURL]`; `:evaluate(assessorDID,grade,criteriaURL)` | Yes — caller branch omitted | EvidenceNet.t_Reject consumes p_UnderReview without calling eo:evaluate. | Object remains p_SubmittedLMS permanently. |
| evaluate/4; co:evaluate(grade,passing_threshold,currentTime,expiryDate) ↔ :evaluate(grade,passing_threshold,currentTime,expiryDate) | CompetencyNet.rnw | t_Review_Init | CredentialObject.rnw | t_StartReview | p_Submitted → p_UnderReview; input `[issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; output `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; `:submit(w,l,h,grade,passing_threshold,currentTime,expiryDate); co :new CredentialObject; co:evaluate(grade,passing_threshold,currentTime,expiryDate)` | p_Submitted → p_UnderReview; input `[issuerDID,studentDID,credentialData,studentSignature,issuerSignature,holderDID,vpData]`; output `[grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,studentSignature,issuerSignature,holderDID,vpData]`; `:evaluate(grade,passing_threshold,currentTime,expiryDate)` | No permanent mismatch witnessed | Target reference, receiver, arguments and prerequisite markings match in the recorded invocation. | Released in directed replay; this is not a universal liveness proof. |
| validate/0; co:validate() ↔ :validate() | CompetencyNet.rnw | t_Verification_Pass | CredentialObject.rnw | t_Validate | p_UnderReview → p_Validated; input `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; output `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; `guard grade >= passing_threshold; co:validate()` | p_UnderReview → p_Validated; input `[grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,studentSignature,issuerSignature,holderDID,vpData]`; output `[grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,studentSignature,issuerSignature,holderDID,vpData]`; `:validate(); guard grade >= passing_threshold` | No permanent mismatch witnessed | Target reference, receiver, arguments and prerequisite markings match in the recorded invocation. | Released in directed replay; this is not a universal liveness proof. |
| sbt_mint/3; co:sbt_mint(issuerDID,studentDID,credentialData) ↔ :sbt_mint(issuerDID,studentDID,credentialData) | CompetencyNet.rnw | t_SBT_Mint | CredentialObject.rnw | t_Mint | p_Validated → p_PendingAccept; input `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; output `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; `:sbt_mint(issuerDID,studentDID,credentialData); co:sbt_mint(issuerDID,studentDID,credentialData); w:sbt_mint(this,issuerDID,studentDID,credentialData)` | p_Validated → p_PendingAccept; input `[grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,studentSignature,issuerSignature,holderDID,vpData]`; output `[grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,studentSignature,issuerSignature,holderDID,vpData]`; `:sbt_mint(issuerDID,studentDID,credentialData)` | No permanent mismatch witnessed | Target reference, receiver, arguments and prerequisite markings match in the recorded invocation. | Released in directed replay; this is not a universal liveness proof. |
| sbt_mint/4; w:sbt_mint(this,issuerDID,studentDID,credentialData) ↔ :sbt_mint(c,issuerDID,studentDID,credentialData) | CompetencyNet.rnw | t_SBT_Mint | WalletNet.rnw | t_ReceiveCredential | p_Validated → p_PendingAccept; input `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; output `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; `:sbt_mint(issuerDID,studentDID,credentialData); co:sbt_mint(issuerDID,studentDID,credentialData); w:sbt_mint(this,issuerDID,studentDID,credentialData)` | p_Empty → p_CredentialReceived; input `[]`; output `[c,issuerDID,studentDID,credentialData]`; `:sbt_mint(c,issuerDID,studentDID,credentialData)` | No permanent mismatch witnessed | Target reference, receiver, arguments and prerequisite markings match in the recorded invocation. | Released in directed replay; this is not a universal liveness proof. |
| sbt_accept/1; co:sbt_accept(studentSignature) ↔ :sbt_accept(studentSignature) | CompetencyNet.rnw | t_SBT_Accept | CredentialObject.rnw | t_Accept | p_PendingAccept → p_BlockchainAnchored; input `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; output `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; `:sbt_accept(studentSignature); guard currentTime < expiryDate; co:sbt_accept(studentSignature); l:anchor_request()` | p_PendingAccept → p_Anchored; input `[grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,studentSignature,issuerSignature,holderDID,vpData]`; output `[grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,studentSignature,issuerSignature,holderDID,vpData]`; `:sbt_accept(studentSignature); guard currentTime < expiryDate` | Yes — transitive loss or argument mismatch | Cancel/expire removes Competency/credential pending token; bad student signature fails CredentialObject equality. Acceptance also requires currentTime < expiryDate. | Student/Wallet decision chain cannot fire; rejection is also blocked by a wrong signature. |
| anchor_request/0; l:anchor_request() ↔ :anchor_request() | CompetencyNet.rnw | t_SBT_Accept | HEDULedgerNet.rnw | t_Execute | p_PendingAccept → p_BlockchainAnchored; input `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; output `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; `:sbt_accept(studentSignature); guard currentTime < expiryDate; co:sbt_accept(studentSignature); l:anchor_request()` | p_Received → p_Executed; input `[]`; output `[]`; `:anchor_request()` | No unfinished caller at the tested skipped invocation | Without acceptance there is no transaction request. | Ledger p_Received remains an inactive readiness token. |
| sbt_reject/1; co:sbt_reject(studentSignature) ↔ :sbt_reject(studentSignature) | CompetencyNet.rnw | t_SBT_Reject | CredentialObject.rnw | t_Reject | p_PendingAccept → p_Terminated; input `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; output `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; `:sbt_reject(studentSignature); co:sbt_reject(studentSignature)` | p_PendingAccept → p_Rejected; input `[grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,studentSignature,issuerSignature,holderDID,vpData]`; output `[grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,studentSignature,issuerSignature,holderDID,vpData]`; `:sbt_reject(studentSignature)` | Yes — transitive loss or argument mismatch | Cancel/expire removes Competency/credential pending token; bad student signature fails CredentialObject equality. Acceptance also requires currentTime < expiryDate. | Student/Wallet decision chain cannot fire; rejection is also blocked by a wrong signature. |
| sbt_cancel/1; co:sbt_cancel(issuerSignature) ↔ :sbt_cancel(issuerSignature) | CompetencyNet.rnw | t_SBT_Cancel | CredentialObject.rnw | t_Cancel | p_PendingAccept → p_Terminated; input `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; output `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; `co:sbt_cancel(issuerSignature)` | p_PendingAccept → p_Cancelled; input `[grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,studentSignature,issuerSignature,holderDID,vpData]`; output `[grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,studentSignature,issuerSignature,holderDID,vpData]`; `:sbt_cancel(issuerSignature)` | Not in tested fixtures; mismatch possible | Invalid issuer signature is unsupported by the existing harness (NOT MEASURED); a different fixed string cannot unify with CredentialObject. | Current correct issuer string permits cancellation; this is a local Competency action, not a University authentication protocol. |
| expire/0; co:expire() ↔ :expire() | CompetencyNet.rnw | t_SBT_Expire | CredentialObject.rnw | t_Expire | p_PendingAccept → p_Terminated; input `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; output `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; `guard currentTime >= expiryDate; co:expire()` | p_PendingAccept → p_Expired; input `[grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,studentSignature,issuerSignature,holderDID,vpData]`; output `[grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,studentSignature,issuerSignature,holderDID,vpData]`; `:expire(); guard currentTime >= expiryDate` | Guard-dependent, not a missing endpoint | Fixed 0 < 100 prevents expiry indefinitely in nominal data; explicit expiry fixture uses 100 >= 100. | Elapsed wall time cannot release this branch. Cancel or valid accept/reject may still be enabled. |
| update_profile/2; co:update_profile(holderDID,vpData) ↔ :update_profile(holderDID,vpData) | CompetencyNet.rnw | t_Agg_Ingest | CredentialObject.rnw | t_Aggregate | p_BlockchainAnchored → p_ProfileAggregated; input `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; output `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; `co:update_profile(holderDID,vpData); l:update_profile(holderDID,vpData); h:update_profile(holderDID,vpData)` | p_Anchored → p_Aggregated; input `[grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,studentSignature,issuerSignature,holderDID,vpData]`; output `[grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,studentSignature,issuerSignature,holderDID,vpData]`; `:update_profile(holderDID,vpData)` | Yes — shared binding lost | Ledger rejection replaces p_Ordered with sink p_Rejected; no p_Committed token can be produced. | The entire Competency/CredentialObject/Ledger/HR aggregation is disabled, including otherwise ready receivers. |
| update_profile/2; l:update_profile(holderDID,vpData) ↔ :update_profile(holderDID,vpData) | CompetencyNet.rnw | t_Agg_Ingest | HEDULedgerNet.rnw | t_Anchor | p_BlockchainAnchored → p_ProfileAggregated; input `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; output `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; `co:update_profile(holderDID,vpData); l:update_profile(holderDID,vpData); h:update_profile(holderDID,vpData)` | p_Committed → p_Anchored; input `[]`; output `[holderDID,vpData]`; `:update_profile(holderDID,vpData)` | Yes — shared binding lost | Ledger rejection replaces p_Ordered with sink p_Rejected; no p_Committed token can be produced. | The entire Competency/CredentialObject/Ledger/HR aggregation is disabled, including otherwise ready receivers. |
| update_profile/2; h:update_profile(holderDID,vpData) ↔ :update_profile(holderDID,vpData) | CompetencyNet.rnw | t_Agg_Ingest | HRAgent.rnw | t_BuildProfile | p_BlockchainAnchored → p_ProfileAggregated; input `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; output `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; `co:update_profile(holderDID,vpData); l:update_profile(holderDID,vpData); h:update_profile(holderDID,vpData)` | p_Parsed → p_CompetencyProfile; input `[]`; output `[holderDID,vpData]`; `:update_profile(holderDID,vpData)` | Yes — shared binding lost | Ledger rejection replaces p_Ordered with sink p_Rejected; no p_Committed token can be produced. | The entire Competency/CredentialObject/Ledger/HR aggregation is disabled, including otherwise ready receivers. |
| sbt_accept/1; c:sbt_accept(studentSignature) ↔ :sbt_accept(studentSignature) | WalletNet.rnw | t_AcceptCredential | CompetencyNet.rnw | t_SBT_Accept | p_PendingConsent → p_Accepted; input `[c,issuerDID,studentDID,credentialData]`; output `[c,issuerDID,studentDID,credentialData]`; `:sbt_accept(studentSignature); c:sbt_accept(studentSignature)` | p_PendingAccept → p_BlockchainAnchored; input `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; output `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; `:sbt_accept(studentSignature); guard currentTime < expiryDate; co:sbt_accept(studentSignature); l:anchor_request()` | Yes — transitive loss or argument mismatch | Cancel/expire removes Competency/credential pending token; bad student signature fails CredentialObject equality. Acceptance also requires currentTime < expiryDate. | Student/Wallet decision chain cannot fire; rejection is also blocked by a wrong signature. |
| sbt_reject/1; c:sbt_reject(studentSignature) ↔ :sbt_reject(studentSignature) | WalletNet.rnw | t_RejectCredential | CompetencyNet.rnw | t_SBT_Reject | p_PendingConsent → p_Rejected; input `[c,issuerDID,studentDID,credentialData]`; output `[c,issuerDID,studentDID,credentialData]`; `:sbt_reject(studentSignature); c:sbt_reject(studentSignature)` | p_PendingAccept → p_Terminated; input `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; output `[co,w,l,h,grade,passing_threshold,currentTime,expiryDate,issuerDID,studentDID,credentialData,issuerSignature,holderDID,vpData]`; `:sbt_reject(studentSignature); co:sbt_reject(studentSignature)` | Yes — transitive loss or argument mismatch | Cancel/expire removes Competency/credential pending token; bad student signature fails CredentialObject equality. Acceptance also requires currentTime < expiryDate. | Student/Wallet decision chain cannot fire; rejection is also blocked by a wrong signature. |

## Dynamic failures despite complete endpoint declarations

Acceptance is Student → Wallet → Competency → CredentialObject **and Ledger.t_Execute**, in one synchronization. Rejection is Student → Wallet → Competency → CredentialObject. Cancel/expire only involve Competency and CredentialObject, so those paths bypass notification to the two decision peers. `sbt_reject` has no expiry guard: a correctly signed rejection can still occur at the expiry fixture before a cancel/expire transition wins; this does not repair peers once the pending token is gone.

Aggregation is Competency → CredentialObject, Ledger and HR in one transaction. After ledger rejection its absent committed token disables all of them. No independent HR update or object aggregate can bypass that missing receiver. Student share can precede aggregation, so the successful path contains no circular dependency here.

The rejected evidence object has a declared receiver with no future invocation; it is a missing failure-path call, not an unknown channel name. Conditional uninvoked ledger/HR services are reported separately from unfinished requesters. No literal fully-enabled-but-unsynchronizable transition was observed.

Signature comparisons and DID/credential identity equality are token unification, not signature verification. Invalid issuer cancellation has no existing scenario selector and was not executed. Full channel liveness across all executions remains NOT MEASURED.
